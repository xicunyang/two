server {
    listen       80;
    server_name  localhost;
    # 原来的配置，匹配根路径
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }
    location /api {
    	proxy_pass http://118.25.194.49:6001/mutou;
    }
}
